'use strict';

var React = require('react/addons');

require('styles/ThList.css');

var ThList = React.createClass({
  render: function () {
    return (
        <div>
          <p>Content for ThList</p>
        </div>
      );
  }
});

module.exports = ThList; 

